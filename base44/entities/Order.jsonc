{
  "name": "Order",
  "type": "object",
  "properties": {
    "order_number": {
      "type": "string",
      "description": "Auto-generated, format MNY-00001"
    },
    "order_date": {
      "type": "string",
      "format": "date-time"
    },
    "customer_id": {
      "type": "string"
    },
    "customer_name": {
      "type": "string"
    },
    "customer_phone": {
      "type": "string"
    },
    "customer_email": {
      "type": "string",
      "format": "email",
      "description": "Email address for order confirmation"
    },
    "city": {
      "type": "string"
    },
    "district": {
      "type": "string"
    },
    "street": {
      "type": "string"
    },
    "building": {
      "type": "string"
    },
    "floor": {
      "type": "string"
    },
    "apartment": {
      "type": "string"
    },
    "landmark": {
      "type": "string"
    },
    "shipping_zone_id": {
      "type": "string",
      "description": "Link to ShippingZone entity"
    },
    "channel": {
      "type": "string",
      "enum": [
        "Website",
        "Instagram",
        "Facebook",
        "WhatsApp",
        "Other"
      ],
      "default": "Website"
    },
    "utm_source": {
      "type": "string",
      "description": "Normalized (trimmed/lowercase) campaign source captured from landing URL."
    },
    "utm_medium": {
      "type": "string",
      "description": "Normalized (trimmed/lowercase) campaign medium captured from landing URL."
    },
    "utm_campaign": {
      "type": "string",
      "description": "Normalized (trimmed/lowercase) campaign name captured from landing URL."
    },
    "utm_content": {
      "type": "string",
      "description": "Normalized (trimmed/lowercase) campaign content captured from landing URL."
    },
    "utm_term": {
      "type": "string",
      "description": "Normalized (trimmed/lowercase) campaign term captured from landing URL."
    },
    "utm_landing_path": {
      "type": "string",
      "description": "Path where the tracked UTM values were captured."
    },
    "utm_landing_url": {
      "type": "string",
      "description": "Full URL where the tracked UTM values were captured."
    },
    "utm_captured_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO timestamp when UTM attribution was captured."
    },
    "subtotal_usd": {
      "type": "number",
      "default": 0
    },
    "discount_usd": {
      "type": "number",
      "default": 0,
      "description": "Order-level discount amount applied to the subtotal (computed from discount_type + discount_value)."
    },
    "discount_type": {
      "type": "string",
      "enum": [
        "fixed_amount",
        "percentage"
      ],
      "default": "fixed_amount",
      "description": "Whether the order-level discount value is a fixed $ amount or a % of subtotal."
    },
    "discount_value": {
      "type": "number",
      "default": 0,
      "description": "Raw order-level discount value entered by the admin ($ or %, per discount_type)."
    },
    "total_overridden": {
      "type": "boolean",
      "default": false,
      "description": "True when the admin manually overrode the auto-calculated grand total on a manual order."
    },
    "delivery_fee_usd": {
      "type": "number",
      "default": 0
    },
    "grand_total_usd": {
      "type": "number",
      "default": 0
    },
    "currency": {
      "type": "string",
      "default": "USD"
    },
    "payment_method": {
      "type": "string",
      "enum": [
        "Cash on Delivery",
        "Whish"
      ],
      "default": "Cash on Delivery"
    },
    "order_status": {
      "type": "string",
      "enum": [
        "New",
        "Confirmed",
        "Packed",
        "Out for Delivery",
        "Delivered",
        "Cancelled"
      ],
      "default": "New"
    },
    "stock_reserved": {
      "type": "boolean",
      "default": false,
      "description": "True when inventory is held (qty_reserved incremented) at order placement, before confirmation. Prevents double-reservation and marks non-legacy orders."
    },
    "stock_committed": {
      "type": "boolean",
      "default": false,
      "description": "True when stock has been deducted (qty_on_hand) for this order at confirmation. Prevents double-deduction."
    },
    "promo_code": {
      "type": "string"
    },
    "notes": {
      "type": "string"
    }
  },
  "required": [
    "customer_name",
    "customer_phone",
    "customer_email",
    "grand_total_usd"
  ]
}